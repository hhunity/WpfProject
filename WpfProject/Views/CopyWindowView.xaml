<Window x:Class="WpfProject.Views.CopyWindowView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfProject.Views"
        mc:Ignorable="d"
        Title="CopyWindowView" Height="800" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <GroupBox Grid.Row="0" Header="oriinal" Margin="8">
            <StackPanel Orientation="Vertical">
                <StackPanel Orientation="Horizontal">
                    <Button Content="Open" Command="{Binding CopyBtnCommand}" Width="80" Margin="5,5,5,5"/>
                </StackPanel>
                <local:ScotPlot x:Name="Plot2" Height="300" Margin="8"/>
            </StackPanel>
        </GroupBox>
        <Label Grid.Row="1" Content="↓" HorizontalAlignment="Center" FontSize="24" Margin="0,8,0,8"/>
        <GroupBox Grid.Row="2" Header="destination" Margin="8">
            <StackPanel Orientation="Vertical">
                <StackPanel Orientation="Horizontal">
                    <Button Content="Open" Command="{Binding CopyBtnCommand}" Width="80" Margin="5,5,5,5"/>
                </StackPanel>
                <local:ScotPlot x:Name="Plot3" Height="300" Margin="8"/>
            </StackPanel>
        </GroupBox>
    </Grid>
</Window>
