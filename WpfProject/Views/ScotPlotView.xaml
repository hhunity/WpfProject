<UserControl x:Class="WpfProject.Views.ScotPlotView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:WpfProject.Views"
             mc:Ignorable="d"
             d:DesignWidth="800" d:DesignHeight="450">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Button Content="Copy" Grid.Row="0" Command="{Binding CopyBtnCommand}" />
        <local:ScotPlot Grid.Row="1" x:Name="Plot" Margin="8"/>

        <!-- 折りたたみパネル -->
        <Expander Grid.Row="2" IsExpanded="False" Margin="8,0,8,8">
            <Expander.Header>
                <!-- ボタン風ヘッダー（Spacing は使わず Margin で間隔調整） -->
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="Edit" FontWeight="SemiBold" Margin="0,4,8,4"/>
                    <TextBlock Text="" Foreground="#888" VerticalAlignment="Center"/>
                </StackPanel>
            </Expander.Header>

            <!-- ヘッダー行 + リスト本体を上下に配置 -->
            <Border Padding="8" BorderBrush="#DDD" BorderThickness="1" CornerRadius="6" Background="#FAFAFA">
                <DockPanel LastChildFill="True">
                    <!-- テーブルヘッダ（ItemsControl.Header は使わない） -->
                    <Grid DockPanel.Dock="Top" Margin="0,0,0,6">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="50"/>
                            <ColumnDefinition Width="140"/>
                            <ColumnDefinition Width="140"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="#" FontWeight="Bold"/>
                        <TextBlock Grid.Column="1" Text="X" FontWeight="Bold"/>
                        <TextBlock Grid.Column="2" Text="Y" FontWeight="Bold"/>
                    </Grid>

                    <!-- 本体 -->
                    <!-- 省略: 上のGrid/Expander等はそのまま -->

                    <ScrollViewer MaxHeight="90" VerticalScrollBarVisibility="Auto">
                        <ItemsControl ItemsSource="{Binding ElementName=Plot, Path=Points}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Grid Margin="0,2">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="30"/>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>

                                        <!-- # -->
                                        <TextBlock Grid.Column="0" Text="{Binding Index}" VerticalAlignment="Center" Foreground="#666"/>

                                        <!-- X列：読み取り専用 + ホイール + ▲/▼コマンド -->
                                        <DockPanel Grid.Column="1">
                                            <Grid DockPanel.Dock="Right" Width="Auto" Height="Auto" Margin="1,0,0,0">
                                                <Grid.RowDefinitions>
                                                    <RowDefinition/>
                                                    <RowDefinition/>
                                                </Grid.RowDefinitions>
                                                <RepeatButton Grid.Row="0" FontSize="8" Content="▲" Command="{Binding IncXCommand}"/>
                                                <RepeatButton Grid.Row="1" FontSize="8" Content="▼" Command="{Binding DecXCommand}"/>
                                            </Grid>
                                            <TextBox Width="140" Height="26"
                                 IsReadOnly="True"
                                 Text="{Binding X, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                 HorizontalContentAlignment="Right"
                                 local:MouseWheelNudgeBehavior.IsEnabled="True"
                                 local:MouseWheelNudgeBehavior.Axis="X"/>
                                        </DockPanel>

                                        <!-- Y列 -->
                                        <DockPanel Grid.Column="2">
                                            <Grid DockPanel.Dock="Right" Width="Auto" Height="Auto" Margin="1,0,0,0">
                                                <Grid.RowDefinitions>
                                                    <RowDefinition/>
                                                    <RowDefinition/>
                                                </Grid.RowDefinitions>
                                                <RepeatButton Grid.Row="0" FontSize="8" Content="▲" Command="{Binding IncYCommand}"/>
                                                <RepeatButton Grid.Row="1" FontSize="8" Content="▼" Command="{Binding DecYCommand}"/>
                                            </Grid>
                                            <TextBox Width="140" Height="26"
                                 IsReadOnly="True"
                                 Text="{Binding Y, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                 HorizontalContentAlignment="Right"
                                 local:MouseWheelNudgeBehavior.IsEnabled="True"
                                 local:MouseWheelNudgeBehavior.Axis="Y"/>
                                        </DockPanel>
                                    </Grid>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>
                </DockPanel>
            </Border>
        </Expander>
    </Grid>
</UserControl>